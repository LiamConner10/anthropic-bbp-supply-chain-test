# Repo was force-pushed (history destroyed)

Session 042 closure-test: stolen-CCSR force-push over `main`. The CCSR's
`allow_unrestricted_git_push: true` claim is honored — entire commit history
on `main` was rewritten by an attacker holding only a stolen CCSR.
